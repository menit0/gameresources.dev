---
import { getCollection } from 'astro:content';
import Layout from '../layouts/Layout.astro';
import type { Tool } from 'src/interfaces/Tool';

const engines = await getCollection("engines")

---

<Layout>
	{
		engines.map((engine) => {
			return <p>{engine.data.name}</p>
		})
	}
</Layout>
